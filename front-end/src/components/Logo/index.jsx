import './Logo.scss'
import React from 'react'

/**
 * @component the Logo component who render html using the context to collect specific data
 * @returns {HTMLEment} the html of the bar chart activity component
 */
const Logo = () => {
   return (
      <div>
         <img src="/img/logo.svg" alt="" />
      </div>
   )
}

export default Logo
